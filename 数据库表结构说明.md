# JimuReport 数据库表结构说明

## 一、API相关表

### 1. api_field - API字段配置表
用于存储API接口的字段配置信息，定义API返回数据的字段结构。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| api_id | varchar(36) | API ID，关联api_info表 |
| table_name | varchar(100) | 表名 |
| field_name | varchar(100) | 字段名 |
| alias | varchar(100) | 字段别名 |
| data_type | varchar(50) | 数据类型 |
| is_primary | tinyint(1) | 是否为主键（1：是，0：否） |
| is_required | tinyint(1) | 是否必填（1：是，0：否） |
| description | varchar(200) | 字段描述 |
| database_name | varchar(100) | 数据库名称 |

### 2. api_info - API信息主表
用于存储API接口的基本信息和配置。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| api_name | varchar(100) | API名称 |
| api_path | varchar(200) | API路径，唯一标识 |
| description | text | API描述 |
| create_by | varchar(36) | 创建人 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| status | tinyint(1) | 状态（1：启用，0：禁用） |
| api_type | varchar(20) | API类型：table-表类型，sql-SQL类型 |
| sql_content | text | SQL内容（SQL类型API使用） |
| sql_database_name | varchar(100) | SQL使用的数据源名称（SQL类型API使用） |
| need_pagination | int(11) | 是否需要分页：1-需要，0-不需要 |
| page_size | int(11) | 默认每页记录数 |

### 3. api_param - API参数配置表
用于存储API接口的参数配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| api_id | varchar(36) | API ID，关联api_info表 |
| param_name | varchar(100) | 参数名 |
| param_type | varchar(20) | 参数类型（如：query、path、body） |
| data_type | varchar(50) | 数据类型 |
| default_value | varchar(100) | 默认值 |
| description | varchar(200) | 参数描述 |
| validate_rule | varchar(200) | 验证规则 |

### 4. api_table - API表配置表
用于存储API接口使用的表配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| api_id | varchar(36) | API ID，关联api_info表 |
| table_name | varchar(100) | 表名 |
| alias | varchar(50) | 表别名 |
| join_type | varchar(20) | 关联类型（如：inner join、left join） |
| join_condition | varchar(200) | 关联条件 |
| database_name | varchar(100) | 数据库名称 |

## 二、字典相关表

### 5. jimu_dict - 字典主表
用于存储系统字典的基本信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| dict_name | varchar(100) | 字典名称 |
| dict_code | varchar(100) | 字典编码，唯一标识 |
| description | varchar(255) | 描述 |
| del_flag | int(11) | 删除状态 |
| create_by | varchar(32) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(32) | 更新人 |
| update_time | datetime | 更新时间 |
| type | int(1) | 字典类型0为string,1为number |
| tenant_id | varchar(10) | 多租户标识 |

### 6. jimu_dict_item - 字典项表
用于存储字典的具体项信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| dict_id | varchar(32) | 字典id，关联jimu_dict表 |
| item_text | varchar(100) | 字典项文本 |
| item_value | varchar(100) | 字典项值 |
| description | varchar(255) | 描述 |
| sort_order | int(11) | 排序 |
| status | int(11) | 状态（1启用 0不启用） |
| create_by | varchar(32) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(32) | 更新人 |
| update_time | datetime | 更新时间 |

## 三、报表相关表

### 7. jimu_report - 报表主表（在线excel设计器）
用于存储报表的基本信息和配置。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| code | varchar(50) | 编码，唯一标识 |
| name | varchar(50) | 名称 |
| note | varchar(255) | 说明 |
| status | varchar(10) | 状态 |
| type | varchar(50) | 类型 |
| json_str | longtext | json字符串，存储报表配置 |
| api_url | varchar(255) | 请求地址 |
| thumb | text | 缩略图 |
| create_by | varchar(50) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(50) | 修改人 |
| update_time | datetime | 修改时间 |
| del_flag | tinyint(1) | 删除标识0-正常,1-已删除 |
| api_method | varchar(255) | 请求方法0-get,1-post |
| api_code | varchar(255) | 请求编码 |
| template | tinyint(1) | 是否是模板 0-是,1-不是 |
| view_count | bigint(20) | 浏览次数 |
| css_str | text | css增强 |
| js_str | text | js增强 |
| py_str | text | py增强 |
| tenant_id | varchar(10) | 多租户标识 |
| update_count | int(11) | 乐观锁版本 |
| submit_form | tinyint(1) | 是否填报报表 0不是,1是 |
| is_multi_sheet | tinyint(4) | 是否多sheet报表 1是 0否 |

### 8. jimu_report_category - 报表分类表
用于存储报表的分类信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| name | varchar(100) | 分类名称 |
| parent_id | varchar(32) | 父级id |
| iz_leaf | int(11) | 是否为叶子节点(0 否 1是) |
| source_type | varchar(10) | 来源类型( report 积木报表 screen 大屏 drag 仪表盘) |
| create_by | varchar(32) | 创建人 |
| create_time | timestamp | 创建时间 |
| update_by | varchar(32) | 更新人 |
| update_time | timestamp | 更新时间 |
| tenant_id | varchar(11) | 租户id |
| del_flag | int(11) | 删除状态(0未删除，1已删除，2临时删除) |
| sort_no | int(11) | 排序 |

### 9. jimu_report_data_source - 数据源配置表
用于存储数据源的配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(100) | 数据源名称 |
| report_id | varchar(100) | 报表_id |
| code | varchar(100) | 编码 |
| remark | varchar(200) | 备注 |
| db_type | varchar(10) | 数据库类型 |
| db_driver | varchar(100) | 驱动类 |
| db_url | varchar(500) | 数据源地址 |
| db_username | varchar(100) | 用户名 |
| db_password | varchar(100) | 密码（加密存储） |
| create_by | varchar(50) | 创建人 |
| create_time | datetime | 创建日期 |
| update_by | varchar(50) | 更新人 |
| update_time | datetime | 更新日期 |
| connect_times | int(11) | 连接失败次数 |
| tenant_id | varchar(10) | 多租户标识 |
| type | varchar(10) | 类型(report:报表;drag:仪表盘) |

### 10. jimu_report_db - 数据集配置表
用于存储报表数据集的配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| jimu_report_id | varchar(32) | 主键字段，关联jimu_report表 |
| create_by | varchar(50) | 创建人登录名称 |
| update_by | varchar(50) | 更新人登录名称 |
| create_time | datetime | 创建日期 |
| update_time | datetime | 更新日期 |
| db_code | varchar(32) | 数据集编码 |
| db_ch_name | varchar(50) | 数据集名字 |
| db_type | varchar(32) | 数据源类型 |
| db_table_name | varchar(32) | 数据库表名 |
| db_dyn_sql | longtext | 动态查询SQL |
| db_key | varchar(32) | 数据源KEY |
| tb_db_key | varchar(32) | 填报数据源 |
| tb_db_table_name | varchar(32) | 填报数据表 |
| java_type | varchar(32) | java类数据集类型（spring:springkey,class:java类名） |
| java_value | varchar(255) | java类数据源数值（bean key/java类名） |
| api_url | varchar(255) | 请求地址 |
| api_method | varchar(255) | 请求方法0-get,1-post |
| is_list | varchar(10) | 是否是列表0否1是 默认0 |
| is_page | varchar(10) | 是否作为分页,0:不分页，1:分页 |
| db_source | varchar(255) | 数据源 |
| db_source_type | varchar(50) | 数据库类型 MYSQL ORACLE SQLSERVER |
| json_data | text | json数据，直接解析json内容 |
| api_convert | varchar(255) | api转换器 |
| iz_shared_source | int(11) | 是否为共享数据源(0 否 1 是) |
| jimu_shared_source_id | varchar(32) | 指向共享数据集的id |

### 11. jimu_report_db_field - 数据集字段配置表
用于存储数据集的字段配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| create_by | varchar(50) | 创建人登录名称 |
| create_time | datetime | 创建日期 |
| update_by | varchar(50) | 更新人登录名称 |
| update_time | datetime | 更新日期 |
| jimu_report_db_id | varchar(32) | 数据源ID，关联jimu_report_db表 |
| field_name | varchar(80) | 字段名 |
| field_name_physics | varchar(200) | 物理字段名（文件数据集使用，存的是excel的字段标题） |
| field_text | varchar(50) | 字段文本 |
| widget_type | varchar(50) | 控件类型 |
| widget_width | int(11) | 控件宽度 |
| order_num | int(11) | 排序 |
| search_flag | int(11) | 查询标识0否1是 默认0 |
| search_mode | int(11) | 查询模式1简单2范围 |
| dict_code | varchar(255) | 字典编码支持从表中取数据 |
| search_value | varchar(100) | 查询默认值 |
| search_format | varchar(50) | 查询时间格式化表达式 |
| ext_json | text | 参数配置 |

### 12. jimu_report_db_param - 数据集参数配置表
用于存储数据集的参数配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| jimu_report_head_id | varchar(36) | 动态报表ID |
| param_name | varchar(32) | 参数字段 |
| param_txt | varchar(32) | 参数文本 |
| param_value | varchar(1000) | 参数默认值 |
| order_num | int(11) | 排序 |
| create_by | varchar(50) | 创建人登录名称 |
| create_time | datetime | 创建日期 |
| update_by | varchar(50) | 更新人登录名称 |
| update_time | datetime | 更新日期 |
| search_flag | int(11) | 查询标识0否1是 默认0 |
| widget_type | varchar(50) | 查询控件类型 |
| search_mode | int(11) | 查询模式1简单2范围 |
| dict_code | varchar(255) | 字典 |
| search_format | varchar(50) | 查询时间格式化表达式 |
| ext_json | text | 参数配置 |

### 13. jimu_report_export_job - 积木报表导出计划表
用于存储报表的定时导出任务配置。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| name | varchar(100) | 任务名称 |
| begin_time | datetime | 开始时间 |
| end_time | datetime | 结束时间 |
| exec_interval | varchar(100) | 执行频率 |
| report_conf | text | 导出报表配置 |
| last_run_time | datetime | 最后执行时间 |
| receiver_email | text | 接收通知的邮件 |
| file_sync_path | varchar(255) | 文件同步路径 |
| status | int(11) | 状态(0:停止;1:启动) |
| create_by | varchar(50) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(50) | 修改人 |
| update_time | datetime | 修改时间 |
| tenant_id | varchar(10) | 多租户标识 |

### 14. jimu_report_export_log - 积木报表自动导出记录表
用于存储报表导出的执行记录。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| batch_no | varchar(50) | 批次编号 |
| export_channel | varchar(20) | 导出渠道 |
| export_from | varchar(20) | 发起来源 |
| from_id | varchar(32) | 来源id |
| export_type | varchar(10) | 导出类型 |
| report_id | text | 报表id |
| download_path | varchar(255) | 下载路径 |
| status | varchar(15) | 状态 |
| err_msg | text | 错误消息 |
| create_by | varchar(32) | 创建人 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| tenant_id | varchar(10) | 多租户标识 |

### 15. jimu_report_ext_data - 通用扩展数据表
用于存储系统的扩展数据信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键ID |
| biz_type | varchar(100) | 业务类型标识，如 report_share、temp_config 等 |
| name | varchar(200) | 名称，展示用 |
| descr | varchar(500) | 描述信息 |
| tags | varchar(255) | 标签，多个用逗号分隔 |
| data_value | longtext | 实际存储内容 |
| metadata | varchar(500) | 元数据，用于存储补充信息 |
| status | tinyint(4) | 状态标识：1正常 0禁用 |
| create_by | varchar(50) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(50) | 修改人 |
| update_time | datetime | 修改时间 |

### 16. jimu_report_icon_lib - 积木图库表
用于存储报表使用的图标资源。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| name | varchar(100) | 图片名称 |
| type | varchar(32) | 图片类型 |
| image_url | varchar(255) | 图片地址 |
| create_by | varchar(32) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(32) | 更新人 |
| update_time | datetime | 更新时间 |
| tenant_id | int(11) | 租户id |

### 17. jimu_report_link - 超链接配置表
用于存储报表的超链接配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键id |
| report_id | varchar(32) | 积木设计器id |
| parameter | text | 参数 |
| eject_type | varchar(1) | 弹出方式（0 当前页面 1 新窗口） |
| link_name | varchar(255) | 链接名称 |
| api_method | varchar(1) | 请求方法0-get,1-post |
| link_type | varchar(1) | 链接方式(0 网络报表 1 网络连接 2 图表联动) |
| api_url | varchar(1000) | 外网api |
| link_chart_id | varchar(50) | 联动图表的ID |
| expression | varchar(255) | 表达式 |
| requirement | varchar(255) | 条件 |

### 18. jimu_report_share - 积木报表预览权限表
用于存储报表的分享配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| report_id | varchar(32) | 在线excel设计器id |
| preview_url | varchar(1000) | 预览地址 |
| preview_lock | varchar(4) | 密码锁 |
| last_update_time | datetime | 最后更新时间 |
| term_of_validity | varchar(1) | 有效期(0:永久有效，1:1天，2:7天) |
| status | varchar(1) | 是否过期(0未过期，1已过期) |
| preview_lock_status | varchar(1) | 密码锁状态(0不存在密码锁，1存在密码锁) |
| share_token | varchar(50) | 分享token |

### 19. jimu_report_sheet - 报表Sheet表
用于存储多sheet报表的sheet配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(64) | 主键（Sheet ID） |
| report_id | varchar(64) | 报表ID |
| sheet_name | varchar(255) | Sheet名称 |
| sheet_order | int(11) | 排序（可以为负数，负数表示在默认sheet前面） |
| json_str | longtext | 该sheet的完整jsonStr |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| create_by | varchar(64) | 创建人 |
| update_by | varchar(64) | 更新人 |

## 四、仪表盘相关表

### 20. onl_drag_comp - 组件库
用于存储仪表盘的组件配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| parent_id | varchar(32) | 父级ID |
| comp_name | varchar(50) | 组件名称 |
| comp_type | varchar(20) | 组件类型 |
| icon | varchar(50) | 图标 |
| order_num | int(11) | 排序 |
| type_id | int(11) | 组件类型 |
| comp_config | longtext | 组件配置 |
| status | varchar(2) | 状态0:无效 1:有效 |
| create_by | varchar(50) | 创建人登录名称 |
| create_time | datetime | 创建日期 |
| update_by | varchar(50) | 更新人登录名称 |
| update_time | datetime | 更新日期 |

### 21. onl_drag_dataset_head - 数据集主表
用于存储仪表盘数据集的配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| name | varchar(100) | 名称 |
| code | varchar(36) | 编码 |
| parent_id | varchar(36) | 父id |
| db_source | varchar(100) | 动态数据源 |
| query_sql | varchar(5000) | 查询数据SQL |
| content | varchar(1000) | 描述 |
| iz_agent | varchar(10) | iz_agent |
| data_type | varchar(50) | 数据类型 |
| api_method | varchar(10) | api方法：get/post |
| create_time | datetime | 创建时间 |
| create_by | varchar(50) | 创建人 |
| update_time | datetime | 更新时间 |
| update_by | varchar(50) | 更新人 |
| low_app_id | varchar(32) | 应用ID |
| tenant_id | int(11) | 租户ID |

### 22. onl_drag_dataset_item - 数据集字段配置表
用于存储仪表盘数据集的字段配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| head_id | varchar(36) | 主表ID，关联onl_drag_dataset_head表 |
| field_name | varchar(36) | 字段名 |
| field_txt | varchar(1000) | 字段文本 |
| field_type | varchar(10) | 字段类型 |
| widget_type | varchar(30) | 控件类型 |
| dict_code | varchar(500) | 字典Code |
| dict_table | varchar(125) | 字典表 |
| dict_text | varchar(125) | 字典文本 |
| iz_show | varchar(5) | 是否列表显示 |
| iz_search | varchar(10) | 是否查询 |
| iz_total | varchar(5) | 是否计算总计（仅对数值有效） |
| search_mode | varchar(10) | 查询模式 |
| order_num | int(11) | 排序 |
| create_by | varchar(32) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(32) | 修改人 |
| update_time | datetime | 修改时间 |

### 23. onl_drag_dataset_param - 数据集参数配置表
用于存储仪表盘数据集的参数配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| head_id | varchar(36) | 动态报表ID |
| param_name | varchar(32) | 参数字段 |
| param_txt | varchar(32) | 参数文本 |
| param_value | varchar(1000) | 参数默认值 |
| order_num | int(11) | 排序 |
| iz_search | int(11) | 查询标识0否1是 默认0 |
| widget_type | varchar(50) | 查询控件类型 |
| search_mode | int(11) | 查询模式1简单2范围 |
| dict_code | varchar(255) | 字典 |
| create_by | varchar(50) | 创建人登录名称 |
| create_time | datetime | 创建日期 |
| update_by | varchar(50) | 更新人登录名称 |
| update_time | datetime | 更新日期 |

### 24. onl_drag_page - 仪表盘页面配置表
用于存储仪表盘页面的配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(50) | 主键 |
| name | varchar(100) | 界面名称 |
| path | varchar(100) | 访问路径 |
| background_color | varchar(10) | 背景色 |
| background_image | varchar(255) | 背景图 |
| design_type | int(11) | 设计模式(1:pc,2:手机,3:平板) |
| theme | varchar(10) | 主题色 |
| style | varchar(20) | 面板主题 |
| cover_url | varchar(500) | 封面图 |
| des_json | varchar(1000) | 仪表盘主配置JSON |
| template | longtext | 布局json |
| protection_code | varchar(32) | 保护码 |
| type | varchar(64) | 文件夹类 |
| iz_template | varchar(10) | 是否模板(1:是；0不是) |
| create_by | varchar(50) | 创建人登录名称 |
| create_time | datetime | 创建日期 |
| update_by | varchar(50) | 更新人登录名称 |
| update_time | datetime | 更新日期 |

### 25. onl_drag_page_comp - 页面组件配置表
用于存储仪表盘页面中组件的配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| page_id | varchar(32) | 页面ID |
| comp_id | varchar(32) | 组件ID |
| comp_config | longtext | 组件配置 |
| create_by | varchar(50) | 创建人登录名称 |
| create_time | datetime | 创建日期 |
| update_by | varchar(50) | 更新人登录名称 |
| update_time | datetime | 更新日期 |

### 26. onl_drag_share - 仪表盘预览分享表
用于存储仪表盘的分享配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| drag_id | varchar(32) | 在线仪表盘设计器id |
| preview_url | varchar(1000) | 预览地址 |
| preview_lock | varchar(4) | 密码锁 |
| last_update_time | datetime | 最后更新时间 |
| term_of_validity | varchar(1) | 有效期(0:永久有效，1:1天，7:7天) |
| status | varchar(1) | 是否过期(0未过期，1已过期) |
| preview_lock_status | varchar(1) | 是否为密码锁(0 否,1是) |
| share_token | varchar(32) | 分享token |

### 27. onl_drag_table_relation - 仪表盘聚合表
用于存储仪表盘的多表关联配置信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(50) | 主键 |
| aggregation_name | varchar(100) | 聚合表名称 |
| aggregation_desc | varchar(100) | 聚合表描述 |
| relation_forms | longtext | 关联表单 |
| filter_condition | longtext | 过滤条件 |
| header_fields | longtext | 表头字段 |
| calculate_fields | longtext | 公式字段 |
| validate_info | longtext | 校验信息 |
| del_flag | tinyint(1) | 删除状态(0-正常,1-已删除) |
| low_app_id | varchar(50) | 应用ID |
| tenant_id | int(11) | 租户ID |
| create_by | varchar(50) | 创建人登录名称 |
| create_time | datetime | 创建日期 |
| update_by | varchar(50) | 更新人登录名称 |
| update_time | datetime | 更新日期 |

## 五、示例数据表

### 28. huiyuan_age - 会员年龄统计表
用于存储会员按年龄分组的统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 名称（年龄段） |
| value | varchar(20) | 值（会员数量） |

### 29. huiyuan_fengongsi - 会员分公司统计表
用于存储会员按分公司分组的统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 名称（分公司名称） |
| value | varchar(20) | 值（会员数量） |
| type | varchar(32) | 类型 |

### 30. huiyuan_huoyuedu - 会员活跃度统计表
用于存储会员按时间段分组的活跃度统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 名称（时间段） |
| value | varchar(20) | 值（活跃度） |

### 31. huiyuan_sex - 会员性别统计表
用于存储会员按性别分组的统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 性别 |
| value | varchar(20) | 值（会员数量） |

### 32. huiyuan_work - 会员工作类型统计表
用于存储会员按工作类型分组的统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 姓名 |
| value | varchar(20) | 值 |

### 33. huiyuan_wxtl - 会员微信通统计表
用于存储会员按地区分组的统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 名称（地区） |
| value | varchar(20) | 值 |

### 34. huiyuan_wxtlshuliang - 会员微信通数量统计表
用于存储会员按等级分组的统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 名称（会员等级） |
| value | int(11) | 值（会员数量） |

### 35. huiyuan_xueli - 会员学历统计表
用于存储会员按学历分组的统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 名称（学历） |
| value | varchar(20) | 值（会员数量） |

### 36. rep_demo_dxtj - 示例：大屏统计表
用于存储示例大屏统计数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(36) | 主键 |
| name | varchar(50) | 姓名 |
| gtime | datetime | 雇佣日期 |
| update_by | varchar(50) | 职务 |
| jphone | varchar(125) | 家庭电话 |
| birth | datetime | 出生日期 |
| hukou | varchar(32) | 户口所在地 |
| laddress | varchar(125) | 联系地址 |
| jperson | varchar(32) | 紧急联系人 |
| sex | varchar(32) | 性别 |

### 37. rep_demo_employee - 示例：员工信息表
用于存储示例员工信息数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(10) | 主键 |
| num | varchar(50) | 编号 |
| name | varchar(100) | 姓名 |
| sex | varchar(10) | 性别 |
| birthday | datetime | 出生日期 |
| nation | varchar(30) | 民族 |
| political | varchar(30) | 政治面貌 |
| native_place | varchar(30) | 籍贯 |
| height | varchar(30) | 身高 |
| weight | varchar(30) | 体重 |
| health | varchar(30) | 健康状况 |
| id_card | varchar(80) | 身份证号 |
| education | varchar(30) | 学历 |
| school | varchar(80) | 毕业学校 |
| major | varchar(80) | 专业 |
| address | varchar(100) | 联系地址 |
| zip_code | varchar(30) | 邮编 |
| email | varchar(30) | Email |
| phone | varchar(30) | 手机号 |
| foreign_language | varchar(30) | 外语语种 |
| foreign_language_level | varchar(30) | 外语水平 |
| computer_level | varchar(30) | 计算机水平 |
| graduation_time | datetime | 毕业时间 |
| arrival_time | datetime | 到职时间 |
| positional_titles | varchar(30) | 职称 |
| education_experience | text | 教育经历 |
| work_experience | text | 工作经历 |
| create_by | varchar(32) | 创建人 |
| create_time | datetime | 创建时间 |
| update_by | varchar(32) | 修改人 |
| update_time | datetime | 修改时间 |
| del_flag | tinyint(1) | 删除标识0-正常,1-已删除 |

### 38. rep_demo_gongsi - 示例：公司信息表
用于存储示例公司信息数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int(11) | 主键，自增 |
| gname | varchar(125) | 货品名称 |
| gdata | varchar(255) | 返利 |
| tdata | varchar(125) | 备注 |
| didian | varchar(255) | 地点 |
| zhaiyao | varchar(255) | 摘要 |
| num | varchar(255) | 数量 |

### 39. rep_demo_jianpiao - 示例：简票信息表
用于存储示例简票信息数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int(11) | 主键，自增 |
| bnum | varchar(125) | 班次号 |
| ftime | varchar(125) | 发车时间 |
| sfkong | varchar(125) | 是否空车 |
| kaishi | varchar(125) | 始发站 |
| jieshu | varchar(125) | 终点站 |
| hezairen | varchar(125) | 核载人数 |
| jpnum | varchar(125) | 检票人数 |
| shihelv | varchar(125) | 实核率 |
| s_id | int(11) | 站点ID |

### 40. rep_demo_xiaoshou - 示例：销售信息表
用于存储示例销售信息数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int(11) | 主键，自增 |
| hnum | varchar(125) | 货品编码 |
| hname | varchar(125) | 货品名称 |
| xinghao | varchar(125) | 型号 |
| fahuocangku | varchar(125) | 发货仓库 |
| danwei | varchar(125) | 单位 |
| num | int(11) | 数量 |
| danjia | varchar(125) | 单价 |
| zhekoulv | int(11) | 折扣率 |
| xiaoshoujine | varchar(125) | 销售金额 |
| beizhu | varchar(125) | 备注 |
| s_id | varchar(11) | 销售ID |

### 41. test_customer - 示例：客户信息表
用于存储示例客户信息数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int(11) | 主键，自增 |
| code | varchar(50) | 客户编号 |
| name | varchar(50) | 客户名称 |
| address | varchar(100) | 客户地址 |
| yylx | varchar(2) | 营业类型 |
| zyyw | varchar(255) | 主营业务 |
| clsj | date | 成立时间 |
| fzr | varchar(50) | 负责人 |
| phone | varchar(11) | 手机号 |
| khyj | varchar(255) | 客户意见 |
| xypd | varchar(255) | 信用评定 |
| tbr | varchar(50) | 填表人 |
| depts | varchar(50) | 部门 |

### 42. test_order - 示例：订单信息表
用于存储示例订单信息数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| order_name | varchar(50) | 订单名称 |
| order_no | varchar(50) | 订单编号 |
| order_sign_date | datetime | 订单签订日期 |
| order_delivery_date | datetime | 订单交付日期 |
| order_coms | varchar(50) | 客户名称 |
| order_pers | varchar(50) | 客户联系人 |
| order_phone | varchar(15) | 客户联系方式 |
| fzr | varchar(50) | 负责人 |
| depts | varchar(50) | 负责人部门 |

### 43. test_order_pros - 示例：订单产品表
用于存储示例订单产品信息数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | varchar(32) | 主键 |
| pro_name | varchar(50) | 产品名称 |
| pro_no | varchar(50) | 产品编号 |
| pro_count | varchar(11) | 产品数量 |
| pro_price | decimal(10,2) | 产品单价 |
| pro_unit | varchar(10) | 单位 |
| pro_model | varchar(10) | 型号 |
| main_id | varchar(32) | 外键，关联test_order表 |

### 44. test_resume - 示例：简历信息表
用于存储示例简历信息数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | int(11) | 主键，自增 |
| name | varchar(50) | 姓名 |
| sex | varchar(2) | 性别 |
| birthday | date | 出生日期 |
| phone | varchar(11) | 手机号 |
| email | varchar(50) | 邮箱 |
| address | varchar(255) | 现住址 |
| native_place | varchar(200) | 籍贯 |
| nation | varchar(100) | 民族 |
| political_outlook | varchar(50) | 政治面貌 |
| education | varchar(10) | 学历 |
| graduation_school | varchar(50) | 毕业院校 |
| self_evaluation | varchar(255) | 自我评价 |
| salary_expectation | decimal(10,2) | 期望薪资 |
| edu_experience | varchar(255) | 教育经历 |
| work_experience | varchar(255) | 工作经历 |

### 45. tmp_report_data_1 - 临时报表数据表1
用于存储临时报表数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| monty | varchar(255) | 月份 |
| main_income | decimal(10,2) | 主营收入 |
| total | decimal(10,2) | 总计 |
| his_lowest | decimal(10,2) | 历史最低 |
| his_average | decimal(10,2) | 历史平均 |
| his_highest | decimal(10,2) | 历史最高 |

### 46. tmp_report_data_income - 临时报表收入数据表
用于存储临时报表收入数据。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| biz_income | varchar(100) | 业务收入 |
| bx_jj_yongjin | decimal(10,2) | 保险佣金 |
| bx_zx_money | decimal(10,2) | 保险咨询费 |
| chengbao_gz_money | decimal(10,2) | 承包工资 |
| bx_gg_moeny | decimal(10,2) | 保险广告费 |
| tb_zx_money | decimal(10,2) | 填报咨询费 |
| neikong_zx_money | decimal(10,2) | 内控咨询费 |
| total | decimal(10,2) | 总计 |

## 六、存储过程

### proc_jmdemo - 示例存储过程
用于演示分页查询的存储过程。

**参数：**
- `pageNo` INT - 页码
- `pageSize` INT - 每页大小

**功能：** 实现分页查询rep_demo_dxtj表的数据

## 数据库设计说明

### 核心功能模块
1. **API生成器**：api_info、api_field、api_param、api_table
2. **报表设计器**：jimu_report、jimu_report_db、jimu_report_db_field等
3. **仪表盘设计器**：onl_drag_page、onl_drag_dataset_head等
4. **数据源管理**：jimu_report_data_source
5. **字典管理**：jimu_dict、jimu_dict_item

### 设计特点
1. **多租户支持**：多个表包含tenant_id字段
2. **软删除**：多个表包含del_flag字段
3. **审计字段**：create_by、create_time、update_by、update_time
4. **扩展性**：使用json_str、ext_json等字段存储扩展配置
5. **关联关系**：通过外键和关联ID建立表间关系

### 索引设计
- 主键索引：所有表都有主键
- 唯一索引：如api_path、code等唯一标识
- 普通索引：如report_id、api_id等关联字段
- 组合索引：如(report_id, sheet_order)等
